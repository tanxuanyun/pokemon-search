overwrite: true
schema: 'https://graphql-pokemon2.vercel.app' # Replace with your GraphQL endpoint
documents: 'app/queries/**/*.ts'
generates:
  ./types/generated/graphql.tsx:
    plugins:
      - typescript
      - typescript-operations
      - typescript-react-apollo
